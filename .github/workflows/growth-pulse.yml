name: Scheduled SDK Health Check

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

concurrency:
  group: scheduled-stacks-pix-sdk-health-check
  cancel-in-progress: true

jobs:
  scheduled-verify:
    name: Verify stacks-pix-sdk from npm
    runs-on: ubuntu-latest
    steps:
      - name: Setup Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Prepare clean environment
        run: mkdir -p /tmp/stacks-pix-sdk-check

      - name: Initialize project
        working-directory: /tmp/stacks-pix-sdk-check
        run: npm init -y

      - name: Fetch stacks-pix-sdk from registry
        working-directory: /tmp/stacks-pix-sdk-check
        run: npm install stacks-pix-sdk

      - name: Confirm SDK require succeeds
        working-directory: /tmp/stacks-pix-sdk-check
        run: node -e "const sdk = require('stacks-pix-sdk'); console.log('Scheduled health check: stacks-pix-sdk OK');"

      - name: Show dependency tree
        working-directory: /tmp/stacks-pix-sdk-check
        run: npm ls --all
